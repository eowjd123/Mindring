name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        HOST: ${{ secrets.SERVER_HOST }}
        USERNAME: ${{ secrets.SERVER_USER }}
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /home/ubuntu/mindring
          
          echo "📥 최신 코드 가져오기"
          git pull origin main
          
          echo "📦 패키지 설치"
          npm install
          
          echo "🔨 프로젝트 빌드"
          npm run build
          
          echo "🔄 PM2 재시작"
          pm2 reload mindring || pm2 start npm --name "mindring" -- start
          
          echo "✅ 배포 완료!"
          pm2 status
